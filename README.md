# cvts-analysis

## Setup
```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
 
## DirectoryStructure

cvts-analysis/
├── scripts/        ← all .py files are here 
├── notebooks/      ← all notebooks files are here
├── data/           ← local only (gitignore)
├── venv/           ← local only (gitignore)
├── requirements.txt
├── README.md
├── .gitignore









